services:
  mysql:
    image: mysql:8.4
    container_name: workouts-mysql
    restart: unless-stopped
    env_file:
      - database.env
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data: